<h1 align="center">Speaker Diarization Using OpenAI Whisper</h1>

[![FreePalestine.Dev](https://freepalestine.dev/header/1)](https://freepalestine.dev)

<p align="center">
  <a href="https://github.com/MahmoudAshraf97/whisper-diarization/actions/workflows/test_run.yml">
    <img src="https://github.com/MahmoudAshraf97/whisper-diarization/actions/workflows/test_run.yml/badge.svg"
         alt="Build Status">
  </a>
  <a href="https://github.com/MahmoudAshraf97/whisper-diarization/stargazers">
    <img src="https://img.shields.io/github/stars/MahmoudAshraf97/whisper-diarization.svg?colorA=orange&colorB=orange&logo=github"
         alt="GitHub stars">
  </a>
  <a href="https://github.com/MahmoudAshraf97/whisper-diarization/issues">
        <img src="https://img.shields.io/github/issues/MahmoudAshraf97/whisper-diarization.svg"
             alt="GitHub issues">
  </a>
  <a href="https://github.com/MahmoudAshraf97/whisper-diarization/blob/master/LICENSE">
        <img src="https://img.shields.io/github/license/MahmoudAshraf97/whisper-diarization.svg"
             alt="GitHub license">
  </a>
  <a href="https://img.shields.io/badge/python-3.8%2B-blue">
        <img src="https://img.shields.io/badge/python-3.8%2B-blue"
             alt="Python version">
  </a>
  <a href="https://twitter.com/intent/tweet?text=&url=https%3A%2F%2Fgithub.com%2FMahmoudAshraf97%2Fwhisper-diarization">
    <img src="https://img.shields.io/twitter/url/https/github.com/MahmoudAshraf97/whisper-diarization.svg?style=social" alt="Twitter">
  </a> 
  </a>
  <a href="https://colab.research.google.com/github/MahmoudAshraf97/whisper-diarization/blob/main/Whisper_Transcription_%2B_NeMo_Diarization.ipynb">
    <img src="https://colab.research.google.com/assets/colab-badge.svg" alt="Open in Colab">
  </a>
</p>
<p align="center">
  <a href="https://trendshift.io/repositories/11413" target="_blank">
    <img src="https://trendshift.io/api/badge/repositories/11413" alt="MahmoudAshraf97%2Fwhisper-diarization | #9 Repository of the day" style="width: 250px; height: 55px;" width="250" height="55"/>
  </a>
</p>


# 
Speaker Diarization pipeline based on OpenAI Whisper

<img src="https://github.blog/wp-content/uploads/2020/09/github-stars-logo_Color.png" alt="drawing" width="25"/> **Please, star the project on github (see top-right corner) if you appreciate my contribution to the community!**

## What is it
This repository combines Whisper ASR capabilities with Voice Activity Detection (VAD) and Speaker Embedding to identify the speaker for each sentence in the transcription generated by Whisper. First, the vocals are extracted from the audio to increase the speaker embedding accuracy, then the transcription is generated using Whisper, then the timestamps are corrected and aligned using `ctc-forced-aligner` to help minimize diarization error due to time shift. The audio is then passed into MarbleNet for VAD and segmentation to exclude silences, TitaNet is then used to extract speaker embeddings to identify the speaker for each segment, the result is then associated with the timestamps generated by `ctc-forced-aligner` to detect the speaker for each word based on timestamps and then realigned using punctuation models to compensate for minor time shifts.

## ✨ Features

- 🎤 **Speaker Diarization**: Identify who spoke when in your audio files
- 📝 **Accurate Transcription**: Powered by OpenAI Whisper for high-quality speech-to-text
- 🎯 **Word-Level Timestamps**: Precise timing for each word using forced alignment
- 🔊 **Vocal Separation**: Optional background music/noise removal with Demucs
- 🌍 **Multi-Language Support**: Works with 90+ languages supported by Whisper
- 💾 **Multiple Output Formats**: Export as TXT, SRT subtitles, or JSON
- 🐍 **Easy Python API**: Simple integration into your projects
- 📦 **pip Installable**: Easy installation from GitHub
- ⚡ **GPU Accelerated**: Fast processing with CUDA support


## 🎯 New in Version 2.0

This project has been refactored into a professional, modular architecture:

- **Python API**: Use as a library in your own projects
- **pip Installable**: Install directly from GitHub
- **Three Processing Modes**: Diarization, transcription-only, or timestamps-only
- **Better Memory Management**: Optimized GPU memory usage
- **Type-Safe**: Full type hints and dataclasses
- **Well Documented**: Comprehensive API documentation

See [USAGE.md](USAGE.md) for detailed usage examples and [ARCHITECTURE.md](ARCHITECTURE.md) for technical details.

## Installation

### Requirements

- Python >= `3.8` (Python `3.10+` recommended)
- `FFMPEG` and `Cython` as prerequisites

Install prerequisites:

```bash
# Install Cython
pip install cython

# Or on Ubuntu/Debian
sudo apt update && sudo apt install cython3

# Install FFmpeg
# on Ubuntu or Debian
sudo apt update && sudo apt install ffmpeg

# on Arch Linux
sudo pacman -S ffmpeg

# on MacOS using Homebrew (https://brew.sh/)
brew install ffmpeg

# on Windows using Chocolatey (https://chocolatey.org/)
choco install ffmpeg

# on Windows using Scoop (https://scoop.sh/)
scoop install ffmpeg

# on Windows using WinGet (https://github.com/microsoft/winget-cli)
winget install ffmpeg
```

### Option 1: Install from GitHub (Recommended)

Install the package directly from GitHub:

```bash
pip install git+https://github.com/MahmoudAshraf97/whisper-diarization.git
```

Or install a specific version:

```bash
pip install git+https://github.com/MahmoudAshraf97/whisper-diarization.git@v2.0.0
```

### Option 2: Install from Source (Development)

For development or if you want to modify the code:

```bash
git clone https://github.com/MahmoudAshraf97/whisper-diarization.git
cd whisper-diarization
pip install -e .
```

### Option 3: Manual Installation (Legacy)

If you prefer the old method without package installation:

```bash
git clone https://github.com/MahmoudAshraf97/whisper-diarization.git
cd whisper-diarization
pip install -c constraints.txt -r requirements.txt
```

For detailed installation instructions and troubleshooting, see [INSTALL.md](INSTALL.md).

### Verify Installation

After installation, verify that everything works:

```bash
python test_installation.py
```

Or test directly in Python:

```python
from whisper_diarization import SpeechProcessor

# Check supported languages
langs = SpeechProcessor.get_supported_languages()
print(f"Supported languages: {len(langs)}")

# Create processor
processor = SpeechProcessor(model_name='tiny.en', device='cpu')
print("✓ Installation successful!")
```
## Usage 

### Python API (Recommended)

After installing the package, you can use it as a library in your projects:

```python
from whisper_diarization import SpeechProcessor

# Initialize processor
processor = SpeechProcessor(
    model_name='medium.en',
    device='cuda',
    batch_size=8
)

# Full diarization with speaker identification
result = processor.diarize('audio.mp3')

# Access results
print(f"Detected language: {result.language}")
for segment in result.segments:
    print(f"[{segment.speaker}] {segment.text}")

# Save outputs in different formats
with open('output.txt', 'w') as f:
    f.write(result.to_txt())

with open('output.srt', 'w') as f:
    f.write(result.to_srt())

# Transcription only (faster, no speaker identification)
transcription = processor.transcribe('audio.mp3', language='en')
print(transcription.text)

# Get speaker timestamps only (no transcription)
timestamps = processor.get_timestamps('audio.mp3')
for ts in timestamps:
    print(f"Speaker {ts.speaker_id}: {ts.start_ms}ms - {ts.end_ms}ms")
```

### Command Line Interface (Legacy)

For backward compatibility, the original CLI is still available:

```bash
# Full diarization
python diarize.py -a AUDIO_FILE_NAME
```

If your system has enough VRAM (>=10GB), you can use `diarize_parallel.py` for faster processing:

```bash
python diarize_parallel.py -a AUDIO_FILE_NAME
```

### Python API (New in v2.0)

```python
from processor import SpeechProcessor

# Initialize processor
processor = SpeechProcessor(
    model_name='medium.en',
    device='cuda'
)

# Full diarization
result = processor.diarize('audio.mp3')
print(result.to_txt())

# Save outputs
with open('output.txt', 'w') as f:
    f.write(result.to_txt())

with open('output.srt', 'w') as f:
    f.write(result.to_srt())

# Transcription only (faster)
transcription = processor.transcribe('audio.mp3')

# Timestamps only (fastest)
timestamps = processor.get_timestamps('audio.mp3')
```

See [example.py](example.py) for more usage examples and [test_speech_processor.ipynb](test_speech_processor.ipynb) for interactive testing.

## Command Line Options

### CLI Arguments

- `-a AUDIO_FILE_NAME`: The name of the audio file to be processed
- `--no-stem`: Disables source separation
- `--whisper-model`: The model to be used for ASR, default is `medium.en`
- `--suppress_numerals`: Transcribes numbers in their pronounced letters instead of digits, improves alignment accuracy
- `--device`: Choose which device to use, defaults to "cuda" if available
- `--language`: Manually select language, useful if language detection failed
- `--batch-size`: Batch size for batched inference, reduce if you run out of memory, set to 0 for non-batched inference

### Python API Options

See [USAGE.md](USAGE.md) for detailed Python API documentation and examples.

## Quick Start

```python
# Install the package
pip install git+https://github.com/MahmoudAshraf97/whisper-diarization.git

# Use in your code
from whisper_diarization import SpeechProcessor

processor = SpeechProcessor(device='cuda')
result = processor.diarize('meeting.mp3')

# Save transcription with speaker labels
with open('transcript.txt', 'w') as f:
    f.write(result.to_txt())
```

## Known Limitations
- Overlapping speakers are yet to be addressed, a possible approach would be to separate the audio file and isolate only one speaker, then feed it into the pipeline but this will need much more computation
- There might be some errors, please raise an issue if you encounter any.

## Future Improvements
- Implement a maximum length per sentence for SRT

## Acknowledgements
Special Thanks for [@adamjonas](https://github.com/adamjonas) for supporting this project
This work is based on [OpenAI's Whisper](https://github.com/openai/whisper) , [Faster Whisper](https://github.com/guillaumekln/faster-whisper) , [Nvidia NeMo](https://github.com/NVIDIA/NeMo) , and [Facebook's Demucs](https://github.com/facebookresearch/demucs)

## Citation
If you use this in your research, please cite the project:

```bibtex
@unpublished{hassouna2024whisperdiarization,
  title={Whisper Diarization: Speaker Diarization Using OpenAI Whisper},
  author={Ashraf, Mahmoud},
  year={2024}
}
```
